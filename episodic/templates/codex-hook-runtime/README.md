# Codex Hook Runtime Template

`episodic/scripts/install-bin.sh` が `~/.config/episodic/codex-hook-runtime/` へコピーする runtime 一式です。

Codex の hook は plugin cache の場所に依存しないよう、展開後の `bin/session-start.sh` を呼びます。
